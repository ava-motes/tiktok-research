# Enrichment stack (Whisper / Google Vision / emoji / BigQuery)
# Install: pip install -r requirements-enrichment.txt
#
# Core collection still works with requirements.txt alone.

# Frame sampling for OCR
opencv-python-headless>=4.8.0,<4.12

# Preferred local Whisper (CPU). Falls back to OpenAI Whisper API if missing.
faster-whisper>=1.0.0

# Google Cloud Vision OCR + BigQuery sync
google-cloud-vision>=3.7.0
google-cloud-bigquery>=3.14.0

# Emoji CLDR demojize / names
emoji>=2.10.0

# Research dataset export (scripts/export_research_dataset.py CSV + Parquet)
pandas>=2.0.0
pyarrow>=14.0.0
