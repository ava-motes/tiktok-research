This project queries the TikTok Research API v2.
- Always use Python with the requests library
- Credentials are in .env — never hardcode them
- Use OAuth 2.0 client credentials flow for auth
- Key features: query videos by keyword/hashtag, pull user/account data, export results to CSV
- Always handle pagination and rate limiting
